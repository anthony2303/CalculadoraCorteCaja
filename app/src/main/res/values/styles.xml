<resources>
    <!-- Tema principal de la app, basado en MaterialComponents -->
    <style name="Theme.CorteCaja" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Personaliza colores si quieres -->
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Para que todos los componentes Material usen tu esquema -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>
</resources>
